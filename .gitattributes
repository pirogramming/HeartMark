# Windows에서 작업해도 shell 스크립트는 항상 LF로 유지한다.
# CRLF가 섞이면 리눅스 컨테이너에서 shebang이 깨져
# "entrypoint.sh: no such file or directory" 같은 엉뚱한 에러가 난다.
*.sh text eol=lf

# 컨테이너 안에서 읽히는 설정 파일들도 마찬가지.
Dockerfile text eol=lf
*.conf text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
